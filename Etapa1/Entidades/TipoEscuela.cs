namespace CoreEscuela.Entidades {
    //Los enumerados son un equivalente a tener una lista de enteros. Si no le colocamos un numero, se le asigna en orden 
    public enum TipoEscuela
    {
        Primaria,
        Secundaria,
        Preescolar

    }
}